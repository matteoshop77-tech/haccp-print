# Copia questo file in `.env` (nella root del progetto) e riempi i valori.
# Reperibili in Supabase: Project Settings → API → Project URL / anon public key.
# Il file `.env` è gitignored — non committarlo.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
